import {hiddenTabBarScreens} from './screens';
import Color from './colors';
import {Layout} from './layout';

const appConfig = {
  hiddenTabBarScreens,
  Color,
  Layout,
};

export {
  Color,
  hiddenTabBarScreens,
  Layout,
};

export default appConfig;
